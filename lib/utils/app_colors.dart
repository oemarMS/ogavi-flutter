import 'package:flutter/material.dart';

class AppColors {
  // Primary Colors
  static const Color primary = Color(0xFF6A1B9A);
  static const Color primaryLight = Color(0xFFB39DDB);
  static const Color primaryDark = Color(0xFF4A148C);

  // Background
  static const Color background = Colors.white;
  static const Color backgroundDark = Colors.black;

  // Text
  static const Color textPrimary = Colors.black;
  static const Color textSecondary = Color(0xFF666666);
  static const Color textLight = Colors.white;

  // Caption
  static const Color captionBackground = Color(0xFFD30000);
  static const Color captionText = Colors.white;

  // UI Elements
  static const Color placeholder = Color(0xFFEEEEEE);
  static const Color border = Color(0xFFCCCCCC);

  // Splash Screen
  static const Color splashBackground = Color(
    0xE666C4FA,
  ); // rgba(102, 196, 250, 0.9)

  // Button
  static const Color buttonPrimary = primary;
  static const Color buttonText = textLight;
  static const Color buttonBorder = primary;
}
